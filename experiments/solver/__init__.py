"""experiments/solver: THE SOLVER (campaign ledger row 31; docs/SOLVER.md). a CMA-ES over a small vector of labelled gains on named cell
sets, scored on the honest body against the replay's and the puppet's numbers. a fitted cord is a puppet by construction: NOTHING this
package produces is a result. its use is the best vector, read as a map of where the file's weights sit farthest from walking.

    uv run python experiments/solver/run.py --name NAME --hours H            # a solve (resumable: run the same line again)
    uv run python experiments/solver/run.py --name NAME --time-one           # one evaluation at the file's own vector, timed
"""
TAG = "SOLVER"
